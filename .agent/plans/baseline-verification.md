# Plan: Baseline Verification

## Goal

Establish the repository's clean baseline before extension or report work begins.

## Current State

The lab is a pure-Python project with deterministic data generation, five mission scripts, `verify.py`, and pytest coverage. Generated deliverables are expected under `outputs/`.

## Files Likely Touched

- None for verification-only work.
- `outputs/report.md`, `outputs/savings.png`, and `outputs/focus_export.csv` may be regenerated by verification commands.

## Implementation Steps

1. Confirm Python dependencies are installed from `requirements.txt`.
2. Run `python data/generate.py` if input CSVs are missing or stale.
3. Run `python verify.py` and record the number of passing checks.
4. Run `pytest -q` and record the number of passing tests.
5. If either command fails, inspect the failing mission or test before feature work.

## Acceptance Criteria

- [ ] `python verify.py` reaches 11/11 checks.
- [ ] `pytest -q` reaches 15 passing tests.
- [ ] Generated outputs exist after verification: `outputs/report.md`, `outputs/savings.png`, and `outputs/focus_export.csv`.

## Verification

- [ ] `python verify.py`
- [ ] `pytest -q`

## Risks

- Missing dependencies can cause import errors unrelated to source logic.
- `verify.py` regenerates data and writes outputs, so generated artifacts may change timestamps.

## Completion Notes

Pending.
